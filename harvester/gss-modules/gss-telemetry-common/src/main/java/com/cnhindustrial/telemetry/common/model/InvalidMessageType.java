package com.cnhindustrial.telemetry.common.model;

public enum InvalidMessageType {
    INVALID_VEHICLE_ID,
    TIMESTAMP_AFTER,
    TIMESTAMP_BEFORE,
    INVALID_TIME_FORMAT,
    INVALID_LATITUDE,
    INVALID_LONGITUDE
}
